    <!-- Masthead -->
    <header class="masthead" style="background-image: url(<%= asset_url 'template_ones/creative/backgrounds/bg-header.jpg' %>);">

      <div class="container h-100">
        <div class="row h-100">
          <div class="col-12 my-auto text-center text-white">
            <img class="masthead-img img-fluid mb-3" src="<%= asset_url 'template_ones/creative/profile.svg' %>" alt=""> 
            
            <%= bootstrap_flash %>
            <div class="masthead-title">Vitality</div>
            <hr class="colored">
            <div class="masthead-subtitle">A Portfolio Theme for Creatives</div>
          </div>
        </div>
      </div>
      <div class="scroll-down">
        <a class="btn js-scroll-trigger" href="#about">
          <i class="fa fa-angle-down fa-fw"></i>
        </a>
      </div>
        
    </header>
<%= link_to "Edit Header", edit_header_path, class: "btn btn-outline-info btn-sm pull-right" if current_user.try(:admin?) %> 
 
<%= link_to "Edit Navigation", edit_navigation_path, class: "btn btn-outline-info btn-sm pull-left" if current_user.try(:admin?) %> 